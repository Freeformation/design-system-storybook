import tokens from '../../../tokens';

const light = {
  brand: 'premium',
  mode: 'light',
  colors: {
    background: tokens.colors.neutral['50'],
    surface: '#FFFFFF',
    text: tokens.colors.neutral['900'],
    primary: tokens.colors.secondary['600'],
    secondary: tokens.colors.secondary['400'],
  },
  tokens
};

export default light;
