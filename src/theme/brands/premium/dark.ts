import tokens from '../../../tokens';

const dark = {
  brand: 'premium',
  mode: 'dark',
  colors: {
    background: tokens.colors.neutral['900'],
    surface: tokens.colors.neutral['800'],
    text: tokens.colors.neutral['50'],
    primary: tokens.colors.secondary['400'],
    secondary: tokens.colors.secondary['300'],
  },
  tokens
};

export default dark;
