import tokens from '../../../tokens';

const light = {
  brand: 'tara',
  mode: 'light',
  colors: {
    background: tokens.colors.neutral['50'],
    surface: '#FFFFFF',
    text: tokens.colors.neutral['900'],
    primary: tokens.colors.success['600'],
    secondary: tokens.colors.success['400'],
  },
  tokens
};

export default light;
