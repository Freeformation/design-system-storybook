import tokens from '../../../tokens';

const dark = {
  brand: 'tara',
  mode: 'dark',
  colors: {
    background: tokens.colors.neutral['900'],
    surface: tokens.colors.neutral['800'],
    text: tokens.colors.neutral['50'],
    primary: tokens.colors.success['400'],
    secondary: tokens.colors.success['300'],
  },
  tokens
};

export default dark;
